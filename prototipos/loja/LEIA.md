# loja

Protótipo do curso. Estrutura esperada, definida em `metodo/FORMATO.md`:

```
declaracao.md          a fonte única
dados/
  padrao/              um arquivo por entidade, em JSON, mais o sessao.json
src/                   o layout. Casca, sem a lógica que o aluno vai escrever
```

O que for compartilhado com os outros protótipos vive em `boilerplate/` e é
copiado por cima na montagem do zip. Não duplique aqui.

Apague este arquivo quando o protótipo existir.
